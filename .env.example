# Copy this file to .env.local for local development.
# Never commit real secrets.

# Optional API auth for your GPT Action / site calls
ACTION_API_KEY=replace_with_a_private_key

# Optional future OpenAI API key for backend/serverless use only
OPENAI_API_KEY=replace_with_your_openai_key
